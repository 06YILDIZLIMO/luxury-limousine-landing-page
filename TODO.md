# Image Performance Optimization TODO

- [ ] Update `components/hero.tsx` with optimized `next/image` props (`quality`, `placeholder`, responsive `sizes`, priority only for LCP image)
- [ ] Update `components/services.tsx` image blocks for responsive/lazy-loaded behavior with `quality={75}` and `placeholder="blur"`
- [ ] Optimize `next.config.js` image settings for caching and performance
- [ ] Add `scripts/optimize-images.js` to convert JPG/JPEG assets to WebP using `sharp`
- [ ] Update `package.json` scripts/dependencies for image optimization workflow
- [ ] Run install for new dependency (`sharp`)
- [ ] Commit and push changes to `main`
